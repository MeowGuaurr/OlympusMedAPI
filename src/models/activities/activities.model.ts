import * as mongoose from "mongoose";
import for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}ieldsInterface from "../../interfaces/fieldsForForm/fields.interface";
import { commonSchema } from "models/common.model";

const activities = commonSchema.clone()

const activitiesModel = mongoose.model<for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}ieldsInterface & mongoose.Document>('Activities', activities);

export default activitiesModel;
